[Unit]
Description=powercontrol-server
After=network.target

[Service]
Type=simple
WorkingDirectory=INSTALL_DIR
ExecStart=INSTALL_DIR/run-powercontrol-server.sh
Restart=on-abort

[Install]
WantedBy=multi-user.target
